#include<bits/stdc++.h>
int main()
{
    long long int i,j,n,m,prob[10000];
    char s[10000];
    int c1,c2;
    scanf("%lld %lld",&n,&m);
    c1=c2=0;
    for(i=0;i<=m;i++)
    {
        scanf("%lld",&prob[i]);
        scanf("%s",s);
        if(s[o]=='W'&&s[1]=='A')
        {
            c1++;
        }
        else if(s[o]=='A'&&s[1]=='C')
        {
            c2++;
        }
    }
    return 0;
}
