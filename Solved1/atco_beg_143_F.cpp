#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
ll arr[3*10^5];
int main()
{
    ll n,i;
    cin>>n;
    for(i=0;i<n;i++)
    {
        cin>>arr[i];
    }
    sort(arr,arr+n);
    ll len;
    for(i=0;i<n;i++)
    {
        if(i==0)
        {
            len=0;
        }
        else if(arr[])
    }
    return 0;
}
